export type Van = {
  id: string;
  description: string;
  imageUrl: string;
  name: string;
  price: number;
  type: string;
  hostId: string;
};

export type UserFirebase = {
  id: string;
  phone: string;
  name: string;
  email: string;
  createdAt: string;
};
